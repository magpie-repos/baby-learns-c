/*This program is a corrected version of an example provided as an end of chapter
 * exercise in the book: Programming in C. The program sums several values and
 * prints the result to the terminal.
 */
#include <stdio.h>

int main (void) {
    int sum;
    sum = 25 + 37 - 19;
    printf("The answer is %i.\n", sum);

    return 0;
}
